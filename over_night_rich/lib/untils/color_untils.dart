import 'package:flutter/material.dart';

class ColorUtils {
  // 主要文本颜色，一般用于界面上的主要文字信息
  static const Color primaryTextColor = Color(0xFF212121);

  // 次要文本颜色，一般用于不是特别突出的信息
  static const Color secondaryTextColor = Color(0xFF757575);

  // 背景色，用于界面的主背景
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // 品牌主题色，用于强调元素，如按钮、图标#BC291C
  static const Color themeColor = Color(0xFFCC0002); // Material Design 的红色

  // 分割线颜色，用于列表项、菜单项等的分割线
  static const Color dividerColor = Color(0xFFBDBDBD);

  // 辅助颜色，例如错误、警告、链接等
  static const Color errorColor = Color(0xFFD32F2F);
  static const Color warningColor = Color(0xFFFFC107);
  static const Color linkColor = Color(0xFF1E88E5);

  // 禁用状态颜色，用于不可点击的按钮或文本
  static const Color disabledColor = Color(0xFFBDBDBD);

  // 箭头颜色
  static const Color arrowColor = Color(0xFF333333); // 深灰色

}
